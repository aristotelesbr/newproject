<p id="notice"><%= notice %></p>

<h1>Messages <span id="message_count"><%= Message.count %></span></h1>

<div id="new_message">
  <%= render partial: 'messages/form', locals: { message: Message.new } %>
</div>

<div id="messages" class="mb-5">
  <%= render @messages %>
</div>
