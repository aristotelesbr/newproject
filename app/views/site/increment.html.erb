<turbo-frame id='counter'>
  <div class='mt-4' style="color: #<%= rand_color %>";>
    Current count <%= @counter %>
  </div>
  <div class="mt-4"> 
    <%= link_to "increment", increment_path(counter: @counter), class: 'bg-blue-500 houver:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
  </div>
</turbo-frame>
